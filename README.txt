SETUP

1. Open CMD inside agentic-news-engine

2. Install dependencies:

pip install -r requirements.txt

3. Run:

python main.py

Fast local check without network collection:

python main.py --check-now

Optional Product Hunt source:

set PRODUCTHUNT_TOKEN=<your_product_hunt_developer_token>

OUTPUT

outputs/agentic_updates.xlsx
